# Copyright (c) 2026 amgskobo
# SPDX-License-Identifier: MIT

name: Test

on:
  push:
    branches:
      - main
  pull_request:
  workflow_dispatch:
  schedule:
    # Recheck moving upstream and DYA main branches even without local changes.
    - cron: '56 3 * * 1'

permissions:
  contents: read

concurrency:
  group: test-${{ github.workflow }}-${{ github.head_ref || github.ref_name }}
  cancel-in-progress: true

jobs:
  host:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v5
      - name: Exercise the helpers and every lifted function with full coverage
        run: bash ./tests/run-host-docker.sh
  firmware:
    runs-on: ubuntu-latest
    timeout-minutes: 45
    strategy:
      fail-fast: false
      matrix:
        variant: [upstream, dya]
    steps:
      - uses: actions/checkout@v5
      - name: Build the ${{ matrix.variant }} ZMK firmware fixture
        run: bash ./tests/run-integration-docker.sh ${{ matrix.variant }}
